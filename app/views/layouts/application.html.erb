<!DOCTYPE html>
<html>
  <head>
    <title>NaganoCake</title>
    <meta name="viewport" content="width=device-width,initial-scale=1, maximum-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>

      <div class="container">
        <div class="row">
          <div class="col-4 align-self-center">
            <h3 class="text-nowrap font-weight-bold">ながのCAKE</h3>
          </div>

          <div class="col-2">
          <% if customer_signed_in? %>
            <p>ようこそ</p>
          <% end %>
          </div>

          <div class="col-6 align-self-start">
            <nav class="navbar navbar-light bg-white">
              <lu class="navbar-nav list-group-horizontal">
              <% if admin_signed_in? %>
                <li><%= link_to "商品一覧", admin_items_path, class: "btn btn-outline-secondary text-dark btn-sm text-nowrap" %></li>
                <li><%= link_to "会員一覧", admin_customers_path, class: "ml-3 btn btn-outline-secondary text-dark btn-sm text-nowrap" %></li>
                <li><%= link_to "注文履歴一覧", admin_root_path, class: "ml-3 btn btn-outline-secondary text-dark btn-sm text-nowrap" %></li>
                <li><%= link_to "ジャンル一覧", admin_genres_path, class: "ml-3 btn btn-outline-secondary text-dark btn-sm text-nowrap" %></li>
                <li><%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: "ml-3 btn btn-outline-secondary text-dark btn-sm text-nowrap" %></li>

              <% elsif customer_signed_in? %>
                <li>マイページ</li>
                <li>商品一覧</li>
                <li>カート</li>
                <li>ログアウト</li>

              <% else %>
                <li>About</li>
                <li>商品一覧</li>
                <li>新規登録</li>
                <li>ログイン</li>

              <% end %>
              </lu>
            </nav>
            <div class="p-3 text-right">
            <%= form_with do |f| %>
              <%= f.text_field :serch, placeholder: "Search"%>
            <% end %>
            </div>
          </div>
        </div>
      </div>

    </header>
    <main>
    <%= yield %>
    </main>
    <footer></footer>
  </body>
</html>
